================================================================================
                  carbonyl-agent — Third-Party License Notices
================================================================================

This file aggregates the licenses of every Rust crate redistributed with
the carbonyl-agent wheel (via the carbonyl-fingerprint and carbonyl-wreq
native extensions). It is generated by `scripts/gen-third-party-licenses.sh`
running `cargo about generate`.

The carbonyl-agent source itself is AGPL-3.0-only licensed — see LICENSE at
the repository root.


--------------------------------------------------------------------------------
                              Licenses Overview
--------------------------------------------------------------------------------

{{#each overview}}
  {{name}} ({{count}} crate{{#unless (eq count 1)}}s{{/unless}})
{{/each}}


{{#each licenses}}
================================================================================
                              {{name}}
================================================================================

Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}{{#if crate.repository}}
    {{crate.repository}}{{/if}}
{{/each}}

License text:

{{text}}


{{/each}}
================================================================================
                              End of attributions
================================================================================
